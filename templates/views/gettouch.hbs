{{>navbar}}
<style>
    /* Navbar Styling */
    /* Add your navbar styling here */

    /* Card List Styling */
    body {
        font-family: Arial, sans-serif;
        background-color: #f5f5f5;
    }

    .card-list {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 20px;
        margin-bottom: 20px;
    }
    .card-list {
    margin-top: 20px;
}

    .cards {
        padding: 20px;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        background-color: #fff;
        display: flex;
        align-items: center;
        transition: transform 0.3s ease-in-out;
    }

    .cards:hover {
        transform: translateY(-5px);
    }

    .card-img {
        width: 40px;
        height: 40px;
        margin-right: 20px;
        color: #3498db; /* Icon color */
    }

    .card-text {
        text-align: left;
    }

    .card-text-first {
        font-size: 1.2rem;
        font-weight: bold;
        margin-bottom: 0.5rem;
        color: #333; /* Text color */
    }

    .card-text-next {
        font-size: 1rem;
        color: #666; /* Text color */
    }

    .get-in-touch {
        padding: 20px;
        background-color: #f9f9f9;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    .get-in-heading {
        font-size: 1.8rem;
        font-weight: bold;
        margin-bottom: 1rem;
        color: #333; /* Text color */
    }

    .name-email-phone {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 1rem;
        margin-bottom: 1rem;
    }

    .name-email-phone-input {
        padding: 1rem;
        border: 1px solid #ccc;
        border-radius: 5px;
        font-size: 1rem;
    }

    .textarea {
        padding: 1rem;
        border: 1px solid #ccc;
        border-radius: 5px;
        font-size: 1rem;
        resize: vertical;
        margin-bottom: 1rem;
    }

    .send-msg-btn {
        margin-top: 1rem;
        text-align: center;
    }

    .send-msg-btn button {
        padding: 1rem 2rem;
        background-color: #3498db;
        color: #fff;
        border: none;
        border-radius: 5px;
        font-size: 1.2rem;
        cursor: pointer;
        transition: background-color 0.3s;
        margin-bottom: 30px;
    }

    .send-msg-btn button:hover {
        background-color: #2980b9;
    }

    footer {
        text-align: center;
        padding: 10px;
        background-color: #f8f9fa;
        color: #110e0e;
        position: fixed;
        bottom: 0;
        width: 100%;
    }
</style>


</style>
<div class="container">
    <div class="card-list">
        <div class="phone-card cards">
            <div class="card-img-div">
                <i class="fas fa-phone-alt card-img"></i>
            </div>
            <div class="card-text">
                <p class="card-text-first">
                    Phone
                </p>
                <p class="card-text-next">
                    +91 9932876978
                </p>
            </div>
        </div>
        <div class="email-card cards">
            <div class="card-img-div">
                <i class="fas fa-envelope card-img"></i>
            </div>
            <div class="card-text">
                <p class="card-text-first">
                    Email
                </p>
                <p class="card-text-next">
                    ishikakundu900@gmail.com
                </p>
            </div>
        </div>
        <div class="address-card cards">
            <div class="card-img-div">
                <i class="fas fa-map-marker-alt card-img"></i>
            </div>
            <div class="card-text">
                <p class="card-text-first">
                    Address
                </p>
                <p class="card-text-next">
                    Puruliya, West-Bengal
                </p>
            </div>
        </div>
    </div>
    <div class="get-in-touch">
    <form id="contactForm" action="/contact" method="post">
        <div class="form-input">
            <h1 class="get-in-heading">Get in Touch</h1>
            <div class="name-email-phone">
                <input type="text" name="name" id="nameInput" placeholder="Name" class="name-email-phone-input">
                <input type="email" name="email" id="emailInput" placeholder="Email" class="name-email-phone-input">
                <input type="text" name="phone" id="phoneInput" placeholder="Phone" class="name-email-phone-input">
            </div>
            <div class="text-message">
                <textarea name="message" id="messageInput" cols="30" rows="10" class="textarea" placeholder="Message"></textarea>
            </div>
            <div class="send-msg-btn">
                <button type="submit" id="sendMessageBtn">Send Message via WhatsApp</button>
            </div>
        </div>
    </form>
</div>
                     <!-- Add Font Awesome CSS for Icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css">
  <!-- Add Bootstrap JS and Popper.js Scripts -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
  <!-- Slideshow Script -->
  <script>
    document.getElementById('contactForm').addEventListener('submit', function(event) {
        event.preventDefault();

        // Get form data
        const name = document.getElementById('nameInput').value.trim();
        const email = document.getElementById('emailInput').value.trim();
        const phone = document.getElementById('phoneInput').value.trim();
        const message = document.getElementById('messageInput').value.trim();

        // Construct WhatsApp message
        const whatsappMessage = `Name: ${name}%0AEmail: ${email}%0APhone: ${phone}%0AMessage: ${message}`;

        // Open WhatsApp with prefilled message
        const whatsappURL = `https://wa.me/9932876978/?text=${whatsappMessage}`;
        window.open(whatsappURL, '_blank');
    });
</script>
                </div>
            </div>
        </form>
    </div>
</div>


</div>
{{>curtask}}
{{>footer}}
{{>chat}}